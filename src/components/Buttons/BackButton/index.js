import React from 'react'

import * as S from './styles'

    const BackButton = ({titleBackButton}) => {
    return (
        <S.BackButtonStyled>{titleBackButton}</S.BackButtonStyled>
    );
}

export default BackButton